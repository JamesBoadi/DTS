To run the frontend, navigate to `cd frontend` and follow the readme file

To run the backend, navigate to `cd backend` and follow the readme file

For tests go to `cd backend/Tasks.Test` and run `dotnet test`
